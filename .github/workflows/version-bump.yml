# ⚠️  WORKFLOW DÉPRÉCIÉ - Utilisation manuelle uniquement
# 
# Ce workflow est conservé pour les cas exceptionnels où un bump manuel
# est nécessaire depuis GitHub Actions. 
#
# ✅ MÉTHODE RECOMMANDÉE : Hooks pre-commit locaux
# Les bumps de version se font automatiquement via les hooks pre-commit
# lors des commits feat/fix/perf/refactor.
#
# 📖 Documentation : docs/dev/version-management.md

name: Manual Version Bump (Legacy)

on:
  # Déclenchement automatique désactivé - utiliser les hooks pre-commit locaux
  # push:
  #   branches:
  #     - main
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Type de bump à effectuer'
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - patch
          - minor
          - major
      force_changelog:
        description: 'Forcer la régénération du changelog complet'
        required: false
        default: false
        type: boolean

jobs:
  bump:
    runs-on: ubuntu-latest
    # Workflow manuel uniquement - les bumps automatiques se font via hooks pre-commit
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Fetch all tags and refs
        run: |
          git fetch --all --tags --force
          git tag --sort=-version:refname

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Commitizen
        run: pip install commitizen

      - name: Set git user
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
      
      - name: Display workflow inputs
        run: |
          echo "=== Paramètres du workflow manuel ==="
          echo "Type de bump: ${{ github.event.inputs.bump_type || 'auto' }}"
          echo "Forcer changelog: ${{ github.event.inputs.force_changelog || 'false' }}"
          echo ""
          echo "⚠️  ATTENTION: Ce workflow est déprécié"
          echo "✅ Utilisez plutôt les hooks pre-commit locaux pour les bumps automatiques"
          echo "📖 Voir docs/dev/version-management.md pour plus d'informations"

      - name: Debug information
        run: |
          echo "=== État Git ==="
          git log --oneline -5
          echo -e "\n=== Tags disponibles ==="
          git tag --sort=-version:refname
          echo -e "\n=== Version dans pyproject.toml ==="
          grep "version = " pyproject.toml
          echo -e "\n=== Version commitizen ==="
          cz version --project

      - name: Prepare manual bump
        id: prepare
        run: |
          echo "=== Préparation du bump manuel ==="
          
          # Utiliser le type de bump spécifié ou auto
          BUMP_TYPE="${{ github.event.inputs.bump_type || 'auto' }}"
          FORCE_CHANGELOG="${{ github.event.inputs.force_changelog || 'false' }}"
          
          echo "Type de bump demandé: $BUMP_TYPE"
          echo "Force changelog: $FORCE_CHANGELOG"
          
          # Toujours procéder au bump en mode manuel
          echo "bump_needed=true" >> $GITHUB_OUTPUT
          echo "bump_type=$BUMP_TYPE" >> $GITHUB_OUTPUT
          echo "force_changelog=$FORCE_CHANGELOG" >> $GITHUB_OUTPUT
          
          # Afficher les commits récents pour information
          LAST_TAG=$(git tag --sort=-creatordate | head -1)
          if [ -n "$LAST_TAG" ]; then
            echo ""
            echo "Commits depuis le dernier tag ($LAST_TAG):"
            git log $LAST_TAG..HEAD --oneline --color=always || echo "Aucun commit depuis le dernier tag"
          else
            echo "Aucun tag trouvé - bump initial"
          fi

      - name: Manual version bump
        if: steps.prepare.outputs.bump_needed == 'true'
        run: |
          echo "=== Bump manuel de version ==="
          
          BUMP_TYPE="${{ steps.prepare.outputs.bump_type }}"
          FORCE_CHANGELOG="${{ steps.prepare.outputs.force_changelog }}"
          
          # Forcer la régénération du changelog si demandé
          if [ "$FORCE_CHANGELOG" = "true" ]; then
            echo "🔄 Régénération forcée du changelog complet..."
            cz changelog
          fi
          
          # Effectuer le bump selon le type demandé
          if [ "$BUMP_TYPE" = "auto" ]; then
            echo "🤖 Bump automatique basé sur les commits..."
            if cz bump --yes --changelog; then
              echo "✅ Bump automatique réussi"
            else
              echo "⚠️ Bump automatique échoué, fallback vers patch"
              cz bump --increment patch --yes --changelog
            fi
          else
            echo "🎯 Bump manuel de type: $BUMP_TYPE"
            cz bump --increment "$BUMP_TYPE" --yes --changelog
          fi
          
      - name: Push changes
        if: steps.prepare.outputs.bump_needed == 'true'
        run: |
          echo "📤 Push des changements et tags..."
          git push --follow-tags origin main
          
          NEW_TAG=$(git tag --sort=-version:refname | head -1)
          echo "🎉 Version $NEW_TAG publiée avec succès !"
          echo "🚀 La release automatique devrait se déclencher maintenant"
